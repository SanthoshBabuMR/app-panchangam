App for listing full/no moon for the current year

**Dependencies**
* grunt-contrib-connect

**Launching server from grunt**
* App Server ( [http://localhost:6234/](http://localhost:6234/) )
```bash
grunt appServer
```